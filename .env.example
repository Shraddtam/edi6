# Optional - only needed if you want AI-powered recommendations.
# The app works without this because it falls back to rule-based recommendations.
AI_GATEWAY_API_KEY=""

# Runtime should use the Supabase pooler URL.
DATABASE_URL="postgresql://postgres.PROJECT_REF:PASSWORD@aws-1-ap-south-1.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1&sslmode=require"

# Prisma migrations should use the direct Supabase URL.
DIRECT_URL="postgresql://postgres:PASSWORD@db.PROJECT_REF.supabase.co:5432/postgres?sslmode=require"

# Used to sign httpOnly auth cookies.
AUTH_SECRET="replace-this-with-a-long-random-secret"

# Local Python ML service. The Next.js app can fall back if this is offline.
ML_SERVICE_URL="http://localhost:8001"
ML_MODELS_DIR="D:/TY/EDI/privacy_debt_models-20260520T150333Z-3-001/privacy_debt_models"
